.Take any string
2.Split string into character as array
3.Reverse splitted character in array
4.Join characters into one string
*/
function reversed(str)
{
var spt=str.split("");
console.log(spt);
var rev=spt.reverse("");
console.log(rev);
var joined=rev.join('');
console.log(joined);
}
